#include <stdio.h>

int main() {
    int n,j=1,k=0;

    scanf("%d",&n);
    while(n!=1){
        if(n%2==0){
            n/=2;
            k++;
        }
        else{
            n=(n*3+1)/2;
            k++;
        }
    }

    printf("%d",k);
    return 0;
}
