/*计算公式为(a1+...+an)*11*(n-1)*/
#include <stdio.h>
#include <stdlib.h>

int main(void){
	int n;
	int *a;
	int sum=0;
	
	scanf("%d",&n);
	a=(int *)malloc(sizeof(int)*n);
	for(int i=0;i<n;i++){
		scanf("%d",&(a[i]));
		sum+=a[i];
	}
	printf("%d",sum*11*(n-1));
	
} 
